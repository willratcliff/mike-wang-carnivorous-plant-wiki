# Credentials — never commit
*creds*
*credentials*
*.secret
*.secrets
.env
.env.*

# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/

# Python (likely tooling)
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node (if we use a static site generator)
node_modules/

# Build outputs (placeholder — adjust when site framework is chosen)
_site/
dist/
build/
.cache/

# Local scratch
scratch/
tmp/
